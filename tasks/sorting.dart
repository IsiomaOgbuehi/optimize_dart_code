/*
The following Dart code snippet is intended to sort a list of integers in ascending order using the sort() method. 
However, the list is not being sorted correctly. Identify and fix the issue.
*/

void main() {
  List<int> numbers = [3, 2, 1, 4, 5];
  numbers.sort();
  print(numbers);
}